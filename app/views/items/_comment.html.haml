.right_content
  .header
    .item 
      %h1.item__name 
        = @item.name
        %span<>
        の実
      %p.item__type
        (
        %span<>
        = @item.ability_type.name
        %span<>
        )
      .btn
        .write
          .write__btn
            = link_to new_item_comment_path(params[:id]) do
              Write
        .edit
          = link_to edit_user_path(current_user) do
            Edit
  .comments
    .title
      能力の特徴
    %ul.ability{style: "padding-left: 30px;"}
      %li.ability__unique
        - @comments.each do |c| 
          = c.ability
          %br
    .title
      能力使用者
    %ul.consumer{style: "padding-left: 30px;"}
      %li.consumer__name
        - @comments.each do |c| 
          = c.consumer  
    .title
      必殺技
    %ul.skill{style: "padding-left: 30px;"}
      %li.skill__name
        - @comments.each do |c| 
          = c.skill